{
  "name": "rshooks-e2e",
  "private": true,
  "description": "E2E tests: deploy examples/*/out/*.wasm to a standalone Xahau node and assert runtime behavior.",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "pretest": "node scripts/copy-wasm.mjs",
    "test": "LOG_LEVEL=error vitest run",
    "typecheck": "tsc --noEmit",
    "trace": "docker logs --tail 100 -f xahau 2>&1 | cut -d ' ' -f 5- | grep -a -E 'Publishing ledger|HookInfo|HookTrace|HookError|HookEmit|preclaim result'"
  },
  "dependencies": {
    "@transia/hooks-toolkit": "^2.0.0",
    "dotenv": "^16.4.1",
    "xahau": "^4.0.3"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.5",
    "typescript": "5.9.3",
    "vitest": "^1.3.1"
  }
}
